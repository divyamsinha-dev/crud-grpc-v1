apiVersion: v1
kind: Service
metadata:
  name: grpc-app-service
spec:
  selector:
    app: grpc-app
  type: LoadBalancer
  ports:
    - name: grpc
      protocol: TCP
      port: 50051
      targetPort: 50051
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
